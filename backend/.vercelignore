# Exclude Python cache and virtual environment directories
__pycache__
venv
.venv

# Exclude the uploads folder content entirely
# The app will recreate the 'uploads' folder on the fly if needed
uploads/

# Exclude any other large files or caches if they were temporarily created
*.sqlite
*.db
